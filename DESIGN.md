# DESIGN.md — Industrial AI Hub 设计系统

> 本文件是 AI Agent 的 UI 设计锚点。
> 配合 `AGENTS.md`（技术/开发约束）使用，形成完整的产品规范闭环。
>
> **一句话定位**：这不是一个后台管理系统，这是一个工业 AI 控制中心。

---

## 1. 产品定位

### 1.1 我们是什么

**Industrial AI Hub** — 面向工业场景的 AI 设备管理与智能分析平台。

用户画像：
- 工厂设备运维工程师（日常监控）
- 产线管理者（决策参考）
- 工业自动化集成商（能力展示）
- 技术面试官 / 招聘方（能力评估）

### 1.2 我们不是什么

| 不是 | 区别 |
|------|------|
| 不是 OA 后台 | 不需要审批流、工单、用户管理界面 |
| 不是 SaaS 营销站 | 不需要 hero banner、CTA 按钮、定价页 |
| 不是数据大屏 | 不需要全屏 3D 地球、粒子动画 |
| 不是通用 CRUD 模板 | 每个页面必须体现「工业 + AI」的存在感 |

### 1.3 设计关键词

```
Industrial  ×  AI  ×  Real-time  ×  Professional
```

四个关键词的优先级从高到低。**Professional 是底线**——宁可朴素，不可廉价。

---

## 2. 设计参考

### 2.1 参考方向

| 领域 | 代表 | 借鉴点 |
|------|------|--------|
| SCADA / HMI | Siemens WinCC、Wonderware InTouch | 设备状态色板、仪表盘布局、报警列表 |
| 工业 IoT 平台 | PTC ThingWorx、MindSphere | 设备卡片、KPI 布局、趋势图 |
| 数据密集型 UI | Palantir Foundry、Bloomberg Terminal | 信息密度、数据优先、紧凑排版 |
| 现代控制中心 | NASA 任务控制界面 | 多面板布局、状态指示、专业感 |
| 暗色 Dashboard | Grafana、Datadog | 暗色主题、图表配色、状态卡片 |

### 2.2 不参考

- Ant Design Pro / Element Plus 官方模板（太通用）
- 营销型 Dashboard（太多渐变和装饰）
- 移动端优先的设计（信息密度不够）

---

## 3. 色彩系统

### 3.1 主题：深色模式优先

工业控制系统的主流选择。深色背景让数据更突出，降低长时间监控的视觉疲劳。

```
主背景:       #0f1117  (近黑，冷色调)
面板背景:     #1a1d26  (略浅，用于卡片/面板)
悬浮面板:     #242831  (hover/active 状态)
边框:         #2a2e3a  (低对比度，不抢注意力)
```

### 3.2 文字色

```
主文字:       #e8eaed  (高可读性，非纯白)
次要文字:     #9aa0ac  (标签、说明)
禁用文字:     #5a6070
```

### 3.3 品牌色 / 功能色

```
品牌主色:     #3b82f6  (蓝，信任 + 技术感)
品牌高亮:     #60a5fa  (hover/active)

成功/在线:    #22c55e  (绿)
警告/维护:    #f59e0b  (琥珀)
危险/报警:    #ef4444  (红)
信息/确认:    #3b82f6  (蓝)
```

### 3.4 状态色语义

状态色在工业系统中是**生命线**——操作员必须在 0.5 秒内通过颜色判断设备状态。

| 状态 | 色值 | 含义 | 使用场景 |
|------|------|------|----------|
| 在线/正常 | `#22c55e` | 一切正常 | 设备状态、已解决报警 |
| 离线/未知 | `#6b7280` | 不可用/无数据 | 离线设备 |
| 维护中 | `#f59e0b` | 需要注意 | 维护设备、已确认报警 |
| 报警/异常 | `#ef4444` | 立即处理 | 未处理报警、紧急报警 |

**规则**：状态色必须一致——同一个颜色在所有页面含义相同。

### 3.5 报警等级色

| 等级 | 色值 | 视觉权重 |
|------|------|----------|
| 一般 (L1) | `#60a5fa` | 普通标签 |
| 重要 (L2) | `#f59e0b` | 加粗标签 + 琥珀底色 |
| 紧急 (L3) | `#ef4444` | 加粗标签 + 红色脉冲动画 |

---

## 4. 排版系统

### 4.1 字体

```css
/* UI 字体 */
--font-ui: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;

/* 数据字体（数值、编码、IP 地址等） */
--font-mono: "JetBrains Mono", "Fira Code", "SF Mono", monospace;
```

**规则**：所有数值（KPI、统计、表格数据列）使用 `--font-mono`。这让数据列对齐更整齐，视觉更专业。

### 4.2 字号层级

| 用途 | 字号 | 字重 | 示例 |
|------|------|------|------|
| 页面大标题 | 18px | 600 | "仪表盘" |
| 面板标题 | 14px | 600 | "设备状态分布" |
| KPI 数值 | 32px | 700 | "128" |
| 正文 | 13px | 400 | 表格内容 |
| 辅助文字 | 12px | 400 | 标签、时间戳 |
| 微型标注 | 11px | 500 | 单位、后缀 |

### 4.3 间距

```
紧凑间距: 8px / 12px / 16px
标准间距: 16px / 20px / 24px
宽松间距: 24px / 32px（仅用于页面级分区）
```

**规则**：工业系统偏好紧凑。面板内部用 12-16px，面板之间用 16px。不要留过多空白。

---

## 5. 布局系统

### 5.1 主布局

```
┌──────────────────────────────────────────────────┐
│ Sidebar (56px collapsed / 200px expanded)        │
│ ┌──────────────────────────────────────────────┐ │
│ │ Header (48px)                                │ │
│ ├──────────────────────────────────────────────┤ │
│ │                                              │ │
│ │ Content Area                                 │ │
│ │ (padding: 16px, max-width: none)             │ │
│ │                                              │ │
│ └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

**变更点**（对比当前）：
- Sidebar 宽度收窄：展开 200px（原 220px），折叠 56px（原 64px）
- Header 高度降低：48px（原 60px）
- Content 取消 `max-width: 1280px` 限制——工业系统需要利用全部屏幕宽度

### 5.2 Sidebar 设计

```
深色背景 #0f1117
┌──────────────────┐
│ ◆ Industrial AI  │  ← Logo 区，高度 48px
├──────────────────┤
│ ⊞  控制中心      │  ← 菜单项，高度 40px
│ ⊟  设备管理      │
│ ⊠  报警管理      │  ← 未处理报警 > 0 时，右侧显示红色计数徽章
│ ⊡  操作日志      │
└──────────────────┘
```

- 菜单项高度 40px（原更大），紧凑
- 激活项：左侧 2px 品牌色竖线 + 背景 `#1a1d26`
- 报警管理项右侧显示未处理报警数量（红色徽章）

### 5.3 Header 设计

```
┌──────────────────────────────────────────────────┐
│ ☰  首页 / 控制中心          系统时间  |  admin ▼  │
└──────────────────────────────────────────────────┘
```

- 左侧：折叠按钮 + 面包屑
- 右侧：**系统实时时间**（`HH:mm:ss`，每秒更新，等宽字体）+ 用户头像下拉
- 系统时间的加入让界面有「控制中心」的实时感

---

## 6. 组件规范

### 6.1 KPI 卡片

```
┌─────────────────────────┐
│  ● 设备总数              │  ← 左侧状态点 + 标签
│                         │
│  128                    │  ← 大数值，等宽字体
│  ▲ 12% vs 昨日          │  ← 趋势箭头（可选）
└─────────────────────────┘
```

规范：
- 高度固定 100px
- 背景 `#1a1d26`，左边框 3px 状态色
- 数值 32px / 700 / `--font-mono`
- 标签 12px / 次要文字色
- 趋势行：绿色上箭头 / 红色下箭头 / 灰色持平
- 无图标（当前有图标，工业系统不需要装饰性图标）

### 6.2 状态指示器

```css
/* 在线 — 绿色脉冲 */
.status-online {
  color: #22c55e;
  animation: pulse-green 2s ease-in-out infinite;
}

/* 离线 — 灰色静态 */
.status-offline { color: #6b7280; }

/* 维护中 — 琥珀色呼吸 */
.status-maintenance {
  color: #f59e0b;
  animation: pulse-amber 3s ease-in-out infinite;
}

/* 报警 — 红色快闪 */
.status-alarm {
  color: #ef4444;
  animation: pulse-red 1s ease-in-out infinite;
}
```

用 CSS `box-shadow` 实现发光效果，不用图片。

### 6.3 数据面板（Panel）

```
┌─ 面板标题 ──────────────── 操作区 ─┐
│                                     │
│  内容区                              │
│                                     │
└─────────────────────────────────────┘
```

规范：
- 背景 `#1a1d26`
- 边框 `1px solid #2a2e3a`
- 圆角 6px（工业系统不需要大圆角）
- 标题栏高度 36px，底部分割线
- 标题 13px / 600 / 大写或中文
- 操作区：刷新按钮、筛选器等

### 6.4 表格

规范：
- 深色主题：行背景交替 `#1a1d26` / `#1e2130`
- 表头背景 `#242831`，文字 12px / 600 / 大写
- 数值列使用 `--font-mono`，右对齐
- 状态列使用状态指示器（小圆点 + 文字）
- hover 行背景 `#2a2e3a`
- 紧凑行高 36px

### 6.5 标签（Tag）

工业系统的标签应该更克制：
- 使用小尺寸，低饱和度背景
- 状态标签：状态色 15% 透明度底色 + 状态色文字
- 类型标签：灰色系
- 不使用高饱和度填充色

### 6.6 报警条目

报警是工业系统的核心。每条报警需要一眼看出：
1. **严重性**（颜色：红/琥珀/蓝）
2. **来源设备**（设备名 + 编码）
3. **内容**（报警消息）
4. **时间**（多久前触发，相对时间）
5. **状态**（未处理/已确认/已解决）

### 6.7 图表

ECharts 图表需要适配深色主题：
- 背景透明
- 坐标轴线 `#2a2e3a`
- 标签文字 `#9aa0ac`
- 数据系列使用品牌色板：
  - 蓝色系：`#3b82f6`, `#60a5fa`, `#93c5fd`
  - 绿色系：`#22c55e`, `#4ade80`
  - 红色系：`#ef4444`, `#f87171`
  - 琥珀系：`#f59e0b`, `#fbbf24`
- 面积图使用渐变填充（从 20% 透明度到 0%）
- tooltip 使用深色背景 `#242831`

---

## 7. 页面设计规范

### 7.1 仪表盘（控制中心）

**目标**：打开这个页面，操作员应该能在 3 秒内回答：
- 有多少设备？多少在线？
- 有没有未处理的报警？
- 整体运行状态如何？

**布局**（从上到下）：

```
┌─ 控制中心 ──────────────────── 刷新 ─┐

┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│设备总数│ │在线率 │ │未处理 │ │已解决 │  ← KPI 行（4 列）
│ 128  │ │ 96%  │ │  5   │ │  42  │
└──────┘ └──────┘ └──────┘ └──────┘

┌─ 设备状态矩阵 ──────┐ ┌─ 报警趋势 ──────────┐
│                     │ │                      │
│  环形图 / 状态分布   │ │  柱状图 / 按时间分布  │
│                     │ │                      │
└─────────────────────┘ └──────────────────────┘

┌─ 实时报警流 ────────────────────────────────┐
│ 🔴 [紧急] 一号生产线PLC 温度超限  3分钟前     │
│ 🟡 [重要] 二号传感器 压力偏低    15分钟前     │
│ 🔵 [一般] 三号摄像头 离线         1小时前     │
└─────────────────────────────────────────────┘
```

**关键设计决策**：
- 最近报警改为**报警流（Alarm Feed）**样式，而非表格——更像实时监控
- 每条报警左侧用色条表示等级（红/琥珀/蓝）
- 时间使用相对时间（"3 分钟前"），而非绝对时间
- 空状态文案："所有设备运行正常，无活跃报警"

### 7.2 设备详情（设备画像）

**目标**：从"设备 CRUD 详情"升级为"设备数字孪生画像"。

**布局**：

```
┌─ ← 一号生产线PLC ──── [在线 ●] ──── PLC-001 ─┐

┌─ 设备身份 ──────────┐ ┌─ 实时参数 ──────────┐
│ 类型: PLC           │ │ 温度   72.4 °C  ●   │
│ 编码: PLC-001       │ │ 压力   1.23 MPa ●   │
│ 位置: A 区 3 号工位  │ │ 电流   12.8 A   ●   │
│ IP:   192.168.1.10  │ │ 运行   1456 RPM ●   │
│ 运行: 72 小时        │ │                      │
└─────────────────────┘ └──────────────────────┘

┌─ 运行趋势 ──────────────────────────────────┐
│ [温度] [压力] [电流]  ← Tab 切换             │
│                                             │
│ 折线图（时间轴 + 数值）                       │
│                                             │
└─────────────────────────────────────────────┘

┌─ 统计概览 ──────────────────────────────────┐
│ 数据条数: 1,234  │  均值: 71.2  │  最大: 89.3 │
└─────────────────────────────────────────────┘

┌─ 历史数据 ──────────────────────────────────┐
│ 表格（最近 10 条采集记录）                     │
└─────────────────────────────────────────────┘

┌─ AI 诊断建议（预留） ───────────────────────┐
│  🔮 基于最近 24 小时数据分析：                  │
│  · 温度呈上升趋势，建议关注冷却系统              │
│  · 设备运行稳定，无异常模式                      │
└─────────────────────────────────────────────┘
```

**关键设计决策**：
- 顶部「设备身份」+「实时参数」并排——左侧静态信息，右侧动态数据
- 实时参数每行末尾有状态小圆点（绿色=正常，红色=异常）
- 趋势图使用 Tab 切换（温度/压力/电流），而非全部铺开
- **AI 诊断建议**区域预留（Phase 3 实现，当前显示"AI 分析模块加载中..."占位）
- 统计概览改为水平条，紧凑展示

### 7.3 报警管理

**目标**：从"表格列表"升级为"报警指挥中心"。

**布局**：

```
┌─ 报警管理 ──────────────────── 刷新 ─┐

┌─ 未处理: 5 ─┐ ┌─ 已确认: 3 ─┐ ┌─ 已解决: 42 ─┐  ← 顶部统计条
               │              │               │

┌─ 筛选 ────────────────────────────────────┐
│ [全部状态 ▼]  [全部等级 ▼]  [搜索设备...]  │
└───────────────────────────────────────────┘

┌─ 报警列表 ────────────────────────────────┐
│ 🔴 ┃ [紧急] 温度超限    一号生产线PLC       │
│    ┃ 3分钟前          [确认] [解决]         │
│ ───╋────────────────────────────────────── │
│ 🟡 ┃ [重要] 压力偏低    二号传感器           │
│    ┃ 15分钟前         [确认] [解决]         │
└───────────────────────────────────────────┘
```

**关键设计决策**：
- 顶部增加统计条（未处理/已确认/已解决 三个数字）
- 报警列表改为**卡片流**样式（非表格），每条报警左侧色条表示等级
- 紧急报警（L3）左侧红色色条 + 微弱红色背景
- 操作按钮（确认/解决）仅在 hover 时显示，减少视觉噪音

### 7.4 登录页

**目标**：第一印象传达"工业 AI 平台"的专业感。

**设计方向**：
- 背景：深色 `#0f1117`，叠加微弱的网格线图案（CSS 实现，不用图片）
- 左侧：品牌区（Logo + 产品名 + 一句话描述 + 系统状态指示）
- 右侧：登录表单（白色/深色面板）
- 底部：版本号 + 技术栈标签（Vue 3 / Spring Boot / MySQL）

```
┌──────────────────────────────────────────────────┐
│                                                  │
│  ┌─────────────────┐  ┌──────────────────────┐  │
│  │                 │  │                      │  │
│  │  ◆ Industrial   │  │  欢迎登录             │  │
│  │    AI Hub       │  │                      │  │
│  │                 │  │  [用户名]             │  │
│  │  工业设备智能    │  │  [密码]               │  │
│  │  管理与分析平台  │  │  [ 登 录 ]            │  │
│  │                 │  │                      │  │
│  │  ● 系统运行正常  │  │                      │  │
│  │                 │  │  v1.0 | Spring Boot   │  │
│  └─────────────────┘  └──────────────────────┘  │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 7.5 操作日志

保持当前表格布局，但适配深色主题。日志是审计用途，表格形式最合适。

---

## 8. Element Plus 主题覆盖

通过 CSS 变量覆盖 Element Plus 默认主题以适配深色模式：

```css
/* Element Plus 深色主题覆盖（关键变量） */
:root {
  --el-bg-color: #1a1d26;
  --el-bg-color-overlay: #242831;
  --el-text-color-primary: #e8eaed;
  --el-text-color-regular: #9aa0ac;
  --el-border-color: #2a2e3a;
  --el-fill-color-blank: #1a1d26;
  --el-mask-color: rgba(0, 0, 0, 0.6);
}
```

**规则**：不修改 Element Plus 组件结构，只通过 CSS 变量和 `:deep()` 覆盖样式。

---

## 9. 动效规范

### 9.1 原则

**90% 稳定，10% 视觉增强。**

工业系统不需要花哨动画。动效只用于：
1. 状态变化反馈（hover、点击）
2. 数据加载过渡
3. 报警脉冲（引起注意）

### 9.2 允许的动效

| 场景 | 动效 | 时长 |
|------|------|------|
| 页面切换 | 淡入 + 上移 4px | 200ms |
| 面板加载 | 骨架屏 / 渐显 | 300ms |
| 在线状态 | 绿色脉冲（box-shadow） | 2s 循环 |
| 紧急报警 | 红色脉冲（box-shadow） | 1s 循环 |
| 按钮 hover | 背景色过渡 | 150ms |
| 侧边栏折叠 | 宽度过渡 | 250ms |

### 9.3 禁止的动效

- 页面进入时的大幅度缩放/旋转
- 元素弹跳效果
- 粒子背景动画（登录页除外，且必须微弱）
- 任何影响信息读取的动画

---

## 10. 禁止事项

### 10.1 设计禁止

- ❌ 不使用白色/浅色背景作为主界面（登录页除外）
- ❌ 不使用大圆角（最大 8px）
- ❌ 不使用渐变按钮或渐变卡片背景
- ❌ 不使用装饰性插画或图标（状态指示除外）
- ❌ 不使用超过 3 种字重
- ❌ 不使用纯白文字（用 `#e8eaed`）

### 10.2 技术禁止

- ❌ 不引入新的 UI 框架（保持 Element Plus）
- ❌ 不引入 CSS 预处理器（保持原生 CSS）
- ❌ 不引入动画库（保持 CSS animation/transition）
- ❌ 不修改后端 API 或数据结构
- ❌ 不一次性重写所有页面（按 Phase 逐步推进）
- ❌ 不删除已有功能或业务逻辑

---

## 11. 实施路线图

### Phase 1：设计分析 ✅（当前）

- [x] 当前 UI 问题分析
- [x] 设计方向确定
- [x] DESIGN.md 输出

### Phase 2：Dashboard 升级（控制中心化）

- [ ] 全局深色主题（style.css + Element Plus 变量覆盖）
- [ ] Sidebar / Header 重新设计
- [ ] Dashboard 布局重构（KPI 行 + 图表 + 报警流）
- [ ] 图表深色适配

### Phase 3：Device Detail 升级（设备画像化）

- [ ] 设备身份 + 实时参数并排布局
- [ ] 趋势图 Tab 切换
- [ ] AI 诊断建议区域（预留）
- [ ] 统计数据紧凑展示

### Phase 4：全局优化

- [ ] Alarm 页面升级为报警指挥中心
- [ ] Login 页面重新设计
- [ ] Device List 适配深色主题
- [ ] Operation Log 适配深色主题
- [ ] 全局组件微调（EmptyState、LoadingSpinner）

---

## 12. 设计 Token 速查

```css
/* 背景 */
--iah-bg:           #0f1117;
--iah-panel:        #1a1d26;
--iah-panel-hover:  #242831;
--iah-border:       #2a2e3a;

/* 文字 */
--iah-text:         #e8eaed;
--iah-text-secondary: #9aa0ac;
--iah-text-muted:   #5a6070;

/* 品牌 */
--iah-primary:      #3b82f6;
--iah-primary-light: #60a5fa;

/* 状态 */
--iah-success:      #22c55e;
--iah-warning:      #f59e0b;
--iah-danger:       #ef4444;
--iah-info:         #3b82f6;

/* 字体 */
--font-ui:          "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-mono:        "JetBrains Mono", "Fira Code", "SF Mono", monospace;

/* 间距 */
--space-xs: 4px;
--space-sm: 8px;
--space-md: 12px;
--space-lg: 16px;
--space-xl: 24px;

/* 圆角 */
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
```

---

> 最后更新：2026-08-02 | 维护者：AI 助手 + hula0710
> 本文件随产品迭代持续演进。
