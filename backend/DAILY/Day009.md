# Day 009 - 2026-07-11

## 今日目标
- [x] HashSet 去重 + TreeSet 排序 + 范围操作
- [x] HashMap 3 种遍历（entrySet/keySet/forEach）
- [x] 单词频率统计（merge + 流式排序）
- [x] equals() 与 hashCode() 的契约（Person 类演示）
- [x] LeetCode 217. 存在重复元素（HashSet + 排序双解法）

## 关键收获
- 重写 equals 必须重写 hashCode，否则 HashSet/HashMap 行为异常
- Set.add() 返回 false = 元素已存在，一行判断重复
- TreeSet 有 tailSet/headSet/subSet 范围操作，HashSet 没有
