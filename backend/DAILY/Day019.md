# Day 019 - 2026-07-18

## 今日目标
- [x] MyBatis 注解 CRUD（@Select/@Insert/@Update/@Delete）
- [x] @Results/@ResultMap 字段映射
- [x] 动态 SQL `<script>` + `<if>` + `<foreach>`
- [x] 批量插入 `<foreach>` 3 条
- [x] RowBounds 分页（含 SQL 层 LIMIT 对比）
- [x] Device 表 8→12 条（注解插入+批量插入）

## 关键收获
- 注解方式比 XML 简洁但复杂动态SQL 时 XML 更强
- `<script>` 标签让注解也能写动态 SQL
- `<foreach>` 批量插入时注意 separator=","
- RowBounds 是逻辑分页不如 SQL LIMIT 高效
- 注解 Mapper 注册: `<mapper class="..."/>` 非 `resource`

## MySQL 连接
Docker standalone 3307, database=reboot, 驱动 mysql-connector-j 9.2.0
