# Day 021 - 2026-07-19

## 今日目标
- [x] pom.xml 升级 Spring Boot 3.5 父 POM
- [x] application.yml 统一配置（DataSource + MyBatis + Logging）
- [x] `@SpringBootApplication` 主启动类
- [x] `@Mapper` 接口 + 构造器注入替代手工 SqlSession
- [x] `@RestController` 完整 CRUD（GET/POST/PUT/DELETE）
- [x] 解决 mapper-locations 误扫描 mybatis-config.xml 问题
- [x] LeetCode 704 二分查找
- [x] Spring Boot 启动验证 → 6 个 API 端点全部通过

## 核心变化：独立 MyBatis → Spring Boot

| 对比 | Day 1~20 | Day 21+ |
|------|----------|---------|
| 框架 | 独立 MyBatis | Spring Boot 3.5 |
| DataSource | mybatis-config.xml | application.yml |
| Mapper | session.selectList() | @Mapper + 构造器注入 |
| HTTP | 无 | @RestController + 内嵌 Tomcat |
| 启动 | exec:java | spring-boot:run |

## 新增文件
```
src/main/java/dev/reboot/
├── IndustrialAiHubApplication.java   (@SpringBootApplication)
├── controller/DeviceController.java  (GET/POST/PUT/DELETE)
├── mapper/DeviceMapper.java          (@Mapper 6 个 SQL)
└── entity/Device.java                (POJO)

src/main/resources/
└── application.yml                   (Spring Boot + MyBatis 配置)

src/main/java/code/day21/
├── LeetCode704_BinarySearch.java
└── Day21_SpringBootMigration.java    (学习笔记)
```

## API 端点验证
```
GET    /api/devices               → 6 devices
GET    /api/devices/1             → 温度传感器-A1
GET    /api/devices/type/传感器    → 4 sensors
POST   /api/devices               → created (insert)
PUT    /api/devices/{id}          → updated
DELETE /api/devices/{id}          → deleted
```

## 踩坑记录
1. `mapper-locations: classpath:code/**/*.xml` 会扫描到老 `mybatis-config.xml` → 改为 `*Mapper.xml`
2. mysql-connector-j `runtime` scope 不影响编译（Maven 编译器插件 3.14.0 支持）

## LeetCode
- [x] #704 二分查找 ✅

## Git
- [x] 待提交

## 明日计划
1. MyBatis XML Mapper 集成到 Spring Boot（复用 Day 18/20 的 XML）
2. ResultMap association/collection 在 Spring Boot 中的用法
3. Service 层引入（Controller → Service → Mapper 三层解耦）
4. 全局异常处理 + 统一响应格式
