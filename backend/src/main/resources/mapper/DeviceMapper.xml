<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.reboot.mapper.DeviceMapper">

    <!-- 动态搜索设备（P1-01：支持站点范围过滤，siteIds=null 表示不过滤） -->
    <select id="searchDevices" resultType="dev.reboot.entity.Device">
        SELECT * FROM device
        WHERE is_deleted = 0
        <if test="siteIds != null">
            AND site_id IN
            <foreach collection="siteIds" item="sid" open="(" separator="," close=")">#{sid}</foreach>
        </if>
        <if test="keyword != null and keyword.length() > 0">
            AND (device_name LIKE CONCAT('%', #{keyword}, '%')
                 OR device_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="deviceType != null and deviceType.length() > 0">
            AND device_type = #{deviceType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findByType" resultType="dev.reboot.entity.Device">
        SELECT * FROM device WHERE device_type = #{deviceType} AND is_deleted = 0
        <if test="siteIds != null">
            AND site_id IN
            <foreach collection="siteIds" item="sid" open="(" separator="," close=")">#{sid}</foreach>
        </if>
    </select>

</mapper>
