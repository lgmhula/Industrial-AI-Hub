# ========================
# Industrial AI Hub — Test Profile Config
# 隔离测试环境：不依赖 Redis/RabbitMQ 等外部基础设施
# ========================

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
      - org.redisson.spring.starter.RedissonAutoConfigurationV2
