# Release Gate Final Audit — Baseline V2.1

**审计角色**: Release QA Engineer  
**审计日期**: 2026-08-03 21:09  
**审计范围**: commit `ec9a158` "fix: complete Maven Wrapper + untrack runtime data from Git"  
**审计模式**: 只读，仅确认 B1 和 B2 是否 PASS  
**禁止项**: 不重新大规模扫描，不处理 WARNING

---

## 最终裁决: **GO** ✅

Tag `v2.1.0` 已创建，Baseline V2.1 已冻结。

---

## BLOCK 项验证

### B1: 仓库 HEAD 仍不可构建 → **PASS** ✅

**验证方法**: `git archive HEAD` 模拟干净 checkout → `docker build`

**验证结果**:
- `backend/mvnw` 和 `backend/mvnw.cmd` 均已提交（`git ls-files` 确认）
- HEAD 版 `mvnw` 是官方 Apache Maven Wrapper 3.3.4 脚本：
  - 下载顺序: wget → curl → Java fallback（Downloader.java 编译运行）
  - 无硬编码本机路径（`MAVEN_HOME` 使用 `${MAVEN_USER_HOME}` 变量）
  - `maven-wrapper.jar`（63KB）已预置
- 干净 checkout Docker 构建成功: 镜像 `iah-final-head-verify` 生成，exit 0

**对比上轮**: 上轮 HEAD 版 mvnw 硬编码 `/Users/air/Program/apache-maven-3.9.6` + 依赖 curl 无 fallback → 干净 checkout 构建失败 exit 127。本轮完全修复。

---

### B2: 基础设施运行时数据被 git 跟踪 → **PASS** ✅

**验证方法**: `git ls-files` 检查 ES/MinIO/Nacos 跟踪数 + `.gitignore` 覆盖 + `git status` 清洁度

**验证结果**:
- elasticsearch/minio/nacos 被跟踪文件数: **0**（上轮 232）
- `.gitignore` 新增覆盖: `rabbitmq/`、`elasticsearch/`、`minio/`、`nacos/`
- 运行 compose 后 `git status --short`: **空**（工作区清洁）
- 正确保留的配置文件（9 个）: `mysql/conf/my.cnf`、`mysql/init/*.sql`、`mysql/ms-conf/*.cnf`、`redis/redis.conf`、`redis/sentinel*.conf`、`redis/sentinel-entrypoint.sh`

**Residual（不阻断，延后处理）**: RabbitMQ mnesia 运行时数据 38 文件仍被跟踪（`rabbitmq/mnesia/rabbit@12166dfce31d/...`），但 `.gitignore` 已含 `rabbitmq/` 阻止新增，且不影响 git status 清洁度。

---

## 已冻结状态

| 项 | 值 |
|---|---|
| Git Tag | `v2.1.0` (annotated) |
| 冻结 Commit | `ec9a158` |
| 冻结时间 | 2026-08-03 21:09 |
| 工作区状态 | clean（无未提交变更） |

## 延后处理清单（WARNING，不阻断发布）

1. RabbitMQ mnesia 数据 38 文件残留（`git rm --cached` 清理）
2. RabbitMQ `.erlang.cookie` 权限 0644 → 需 600
3. JwtUtils prod fail-fast（测试 fallback secret）
4. API-Reference 幽灵端点 `/api/alarms/all`
5. README badge `v1.0-alpha` / "Hotfix 中" 状态文本
6. compose 无 profiles 定义，README `--profile core` 无效
