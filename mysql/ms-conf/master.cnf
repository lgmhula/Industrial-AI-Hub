# ========================
# Industrial AI Hub — MySQL Master Configuration
# ========================
[mysqld]
server-id = 1
log-bin = mysql-bin
binlog-format = ROW
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
default-time-zone = '+08:00'
